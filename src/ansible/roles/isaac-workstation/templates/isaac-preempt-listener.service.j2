[Unit]
Description=GCP Spot Preemption Watchdog & Emergency Backup
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /usr/local/bin/preempt-listener.py --results-dir {{ results_dir }}{% if gcs_backup_bucket is defined and gcs_backup_bucket | length > 0 %} --gcs-bucket {{ gcs_backup_bucket }}{% endif %} --deployment-name {{ deployment_name }} --poll-interval 5
Restart=always
RestartSec=5
User=root

[Install]
WantedBy=multi-user.target
