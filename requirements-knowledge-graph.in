# Optional graph runtime only; never installed by the deployment Dockerfile.
# Python 3.12. Compile with uv pip compile --generate-hashes.
rdflib==7.6.0
pyshacl==0.40.1
networkx==3.6.1
python-hcl2==7.3.1
PyYAML==6.0.3
