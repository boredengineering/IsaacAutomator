# IsaacAutomator drift-workflow schema=1 generator=1 sha256=@@DIGEST@@
# INERT until explicitly installed and reviewed. Never commit state or plans.
name: IsaacAutomator report-only drift
'on': @@TRIGGERS@@
permissions:
  contents: read
jobs:
@@JOBS@@
