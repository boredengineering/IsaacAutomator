diff --git a/Dockerfile b/Dockerfile
index 7770f7a..a485363 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,4 +1,4 @@
-FROM node:16.13-alpine3.12 as build
+FROM node:22-bookworm-slim AS build
 
 WORKDIR /usr/src/app
 COPY package*.json ./
@@ -8,15 +8,15 @@ RUN npm install --production \
 COPY . .
 RUN npm run build
 
-FROM node:16.13-alpine3.12 as release
+FROM node:22-bookworm-slim AS release
 
-RUN apk add --no-cache bash curl postgresql-client
+RUN apt-get update && apt-get install -y --no-install-recommends bash curl postgresql-client && rm -rf /var/lib/apt/lists/*
 
 WORKDIR /usr/src/app
 RUN mkdir -p data/products
 
-RUN addgroup -g 1001 -S infracost && \
-  adduser -u 1001 -S infracost -G infracost && \
+RUN groupadd -g 1001 infracost && \
+  useradd -u 1001 -g infracost infracost && \
   chown -R infracost:infracost /usr/src/app
 USER 1001
 
diff --git a/package.json b/package.json
index 5a86ce5..0147404 100644
--- a/package.json
+++ b/package.json
@@ -46,14 +46,14 @@
   "dependencies": {
     "@apollo/server": "^5.5.0",
     "@as-integrations/express4": "1.1.2",
-    "@console/console-platform-log4js-utils": "^4.1.0",
+
     "@graphql-tools/schema": "^9.0.12",
     "@graphql-tools/utils": "^9.1.3",
     "@ibm-cloud/platform-services": "^0.76.3",
     "@types/cors": "^2.8.17",
     "@types/jest": "^29.5.12",
     "@types/lodash": "^4.17.0",
-    "@types/node": "^18.11.8",
+    "@types/node": "^22.0.0",
     "@types/pg": "^8.6.5",
     "@types/pg-copy-streams": "^1.2.2",
     "@types/pg-format": "^1.0.2",
@@ -104,7 +104,7 @@
     "globals": "^15.13.0",
     "nodemon": "^3.1.10",
     "ts-node": "^10.9.2",
-    "typescript": "^4.9.5",
+    "typescript": "^5.7.0",
     "typescript-eslint": "^8.36.0"
   },
   "repository": {
diff --git a/src/config.ts b/src/config.ts
index f9fb888..1af8fc7 100644
--- a/src/config.ts
+++ b/src/config.ts
@@ -1,5 +1,5 @@
 import dotenv from 'dotenv';
-import * as loggerUtils from '@console/console-platform-log4js-utils';
+import * as loggerUtils from 'log4js';
 import NodeCache from 'node-cache';
 import { Pool, PoolConfig } from 'pg';
 import tmp from 'tmp';
diff --git a/src/scrapers/awsBulk.ts b/src/scrapers/awsBulk.ts
index 6ab52b3..cc7025f 100644
--- a/src/scrapers/awsBulk.ts
+++ b/src/scrapers/awsBulk.ts
@@ -138,7 +138,7 @@ async function downloadService(offer: Offer, prefix?: string) {
         `data/${prefix}-${offer.offerCode}-${region.regionCode}.json`
       );
       resp.data.pipe(writer);
-      await new Promise((resolve) => {
+      await new Promise<void>((resolve) => {
         writer.on('finish', resolve);
       });
     }
@@ -153,7 +153,7 @@ async function downloadService(offer: Offer, prefix?: string) {
       `data/${prefix}-${offer.offerCode}.json`
     );
     resp.data.pipe(writer);
-    await new Promise((resolve) => {
+    await new Promise<void>((resolve) => {
       writer.on('finish', resolve);
     });
   }
diff --git a/src/scrapers/gcpCatalog.ts b/src/scrapers/gcpCatalog.ts
index 0259b15..96dae44 100644
--- a/src/scrapers/gcpCatalog.ts
+++ b/src/scrapers/gcpCatalog.ts
@@ -132,7 +132,7 @@ async function downloadService(service: ServiceJson): Promise<void> {
     filename = `data/${filename}.json`;
     const writer = fs.createWriteStream(filename);
     resp.data.pipe(writer);
-    await new Promise((resolve) => {
+    await new Promise<void>((resolve) => {
       writer.on('finish', resolve);
     });
 
