---
uuid: "4e5f6a7b-1029-4112-a102-1827364fa601"
short_uuid: "4e5f6a7b"
timestamp: "2026-08-21T01:55:25Z"
topic: "Implementation of Option A (Official ./isaaclab.sh --conda) & Clean Script Conda Activation Sourcing"
tags: ["option-a-implementation", "official-isaaclab-conda", "conda-profile-sourcing", "isaaclab-install-clean"]
---

# Session Memory: Implementation of Option A (Official ./isaaclab.sh --conda) & Clean Script Conda Activation Sourcing

## Summary
In this session, we added Option A to `isaac-install-plan.md` and implemented it across the installer modules:
1. **Documented in Plan**: Added Section 3.4.2 to `isaac-install-plan.md` detailing the 3 Conda scripting realities (shell functions vs binaries, user context & `envs_dirs`, and clean subshell sourcing) and comparing Option A vs Option B.
2. **Implemented Option A in `lib/modules/conda.sh`**:
   - Automatically purges orphaned `/opt/conda` directories.
   - Delegates Conda environment creation directly to `./isaaclab.sh --conda isaaclab` as target user.
   - Deploys scoped activation hooks in `~/miniconda3/envs/isaaclab/etc/conda/`.
3. **Clean Sourcing in `lib/modules/isaaclab.sh` and `lib/core/state.sh`**:
   - Wrapped `./isaaclab.sh --install` in `source <conda_root>/etc/profile.d/conda.sh` + `conda activate isaaclab` so extensions are compiled and linked directly into the active Conda runtime.
