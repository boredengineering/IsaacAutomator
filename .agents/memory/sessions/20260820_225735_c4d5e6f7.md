---
uuid: "c4d5e6f7-4109-4a21-9988-8291034fec91"
short_uuid: "c4d5e6f7"
timestamp: "2026-08-20T22:57:35Z"
topic: "IsaacLab-Arena 0.3.0, LeRobot Conflict Strategy, Custom Sim Builds, Multi-Version Coexistence, and Auto-Forking"
tags: ["arena-0.3.0", "lerobot-isolation", "custom-sim-build", "multi-version-sim", "auto-fork-gh", "fork-sync-delta"]
---

# Session Memory: Arena 0.3.0, LeRobot Conflict Strategy, Custom Sim Builds, Multi-Version Coexistence, and Auto-Forking

## Summary
In this session, we resolved and implemented key developer configurations and subsystem capabilities across `isaac-installer`:
1. **IsaacLab-Arena 0.3.0-prerelease**: Configured `release/0.3.0-prerelease` targeting Isaac Lab 3.0 / Isaac Sim 6.0.1 with Python 3.12, Newton physics, and OpenPI.
2. **LeRobot Conflict Resolution**: Formalized dedicated `isolated_env: true` (`lerobot` Conda environment) and pinned stable `v0.4.3` to prevent `numpy` and `torchvision` ABI collisions with GR00T (`pandas==2.2.3`).
3. **Custom Source Build Support**: Added `simulation.isaacsim.source_type: "custom_build"` with custom paths and optional compilation hooks in `isaacsim.sh`.
4. **Isaac Sim 6.0.1 and 5.1.0 Multi-Engine Coexistence**: Enhanced `sim switch <version|path>` to seamlessly toggle active standalone / custom engines and matched Conda environments.
5. **Remote Fork Existence & Auto-Creation**: Implemented `ensure_github_fork()` using GitHub CLI (`gh repo fork --clone=false`) and added `check_fork_sync_status()` reporting `ahead`/`behind` commit deltas in `lab status`.

## Next Steps
- Verify execution on target physical workstation.
