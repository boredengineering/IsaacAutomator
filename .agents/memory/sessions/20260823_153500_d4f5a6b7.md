# Session Memory: Pragmatic Dual-Track Strategy (Docker Baseline to Native Parity)

- **Date / Time (UTC):** 2026-08-23 15:35:00
- **Short UUID:** `d4f5a6b7`
- **Topic:** Pragmatic Dual-Track Roadmap (Docker Baseline First -> Reverse-Engineering -> Native Bare-Metal Conversion)
- **Status:** COMPLETED

---

## 1. Summary of Changes & Discoveries

1. **Clarification of Historical Container Baseline**:
   - Recognized that `g1_brainco_extension` was previously executed inside the Docker container (`/workspaces/isaaclab_arena` via `run_docker.sh -g`) because native compilation from source with `uv` was overly complex.
   - Restructured our strategy to start with the **Docker-First Baseline** (proven, reliable execution) to capture telemetry, verify closed-loop inference, and establish a rock-solid operational ground truth.

2. **Extensive Docker Architecture Notes & Runbook**:
   - Documented how `run_docker.sh` and `run_gr00t_server.sh` manage:
     - Base NVIDIA container images (`nvcr.io/nvidia/isaac-sim:6.0.0-dev2`)
     - Internal `/isaac-sim/python.sh` environment binding
     - Pre-compiled C++ WBC kinematic solvers (`pinocchio`, `pin-pink`, `proxsuite`, `quadprog`)
     - Dynamic volume mounts (`-v $(pwd):/workspaces/isaaclab_arena`, `-v $HOME/models:/workspace/models`) for live editable code iteration
     - GPU pass-through (`--gpus all`) and host networking (`--net=host`) for ZeroMQ loopback port 5555.

3. **Reverse-Engineering to Native Conversion Blueprint**:
   - Codified the 3-step extraction procedure:
     1. Dump frozen Python environment (`pip list --format=freeze`)
     2. Map shared libraries and C++ dynamic linkers
     3. Extract runtime environment variables (`LD_LIBRARY_PATH`, `VK_ICD_FILENAMES`)
   - Mapped out the exact Conda (Python 3.12) and UV (Python 3.10) replication steps on the host machine to achieve 100% native bare-metal parity without guesswork.

4. **Master Documents Updated**:
   - `.agents/references/docs/debugging_arena_gr00t.md`: Added Section 6.
   - `.agents/references/docs/isaac-install-plan.md`: Added Section 14.4.

---

## 2. Key Files Modified

- `/workspaces/IsaacAutomator/.agents/references/docs/debugging_arena_gr00t.md`
- `/workspaces/IsaacAutomator/.agents/references/docs/isaac-install-plan.md`
- `/workspaces/IsaacAutomator/.agents/memory/sessions/20260823_153500_d4f5a6b7.md`
- `/workspaces/IsaacAutomator/.agents/memory/INDEX.md`
