---
uuid: "21088c82-4f8c-40e6-8ac8-20d86391a815"
short_uuid: "21088c82"
timestamp: "2026-09-10T03:35:57Z"
topic: "Neo4j evidence projection and advisory agent skill"
tags: ["evidence-graph", "neo4j", "agent-skill", "documentation"]
---

# Session Memory: Neo4j evidence projection and advisory agent skill

## Summary

User selected loading the existing graph into Neo4j for visualization, then
requested a how-to under `.agents/references/docs`, a saved skill implementation
plan under `.agents/references/plans`, and implementation of that plan. Delivered
explicit developer import plus guide, skill and advisory agent brief.

## Decisions & Rationale

- Canonical RDF remains authoritative; Neo4j is a disposable one-way snapshot.
- Agent questions use the bounded freshness-gated CLI, not privileged database
  access. No new model runtime, daemon, global skill install or MCP configuration.
- The skill resides at `.agents/skills/isaac-automator/evidence-graph/SKILL.md`;
  `ai/evidence-graph.agent.md` is its entry point. Explicit reading activates the
  role when a runtime does not automatically discover repository skills.
- Guide: `.agents/references/docs/evidence-graph-agent-guide.md`.
- Implemented plan: `.agents/references/plans/evidence-graph-agent-skill-plan.md`.

## Discovered Gotchas & Caveats

- Protected AGENTS.md discovery edit was denied after approval timeout. It was
  not retried; graph README links and explicit loading provide discovery instead.
- Import scope is derived from absolute checkout path, not policy path. Clear
  accepts `--generation` and optionally the original `--repo`, not `--scope`.
- Neo4j import-time freshness is not continuous synchronization/privacy revocation.
  Lost receipts have unknown commit outcome; clear cannot cancel outstanding work.
- Replacement preserves unrelated nodes but deletes relationships incident to
  owned disposable nodes. Do not attach durable user-authored edges to them.
- Static evidence, SHACL validity and a database smoke test are not runtime success
  or full Graph-RAG evaluation. RDFLib deprecation warnings remain.

## Completed Tasks

- [x] Fixed bounded parameterized receiver, canonical export and CLI load/clear.
- [x] Rebuilt/recreated only dedicated database; kept editor and Arena untouched.
- [x] Actual readback conserved 226 full claim records, 190 entity/literal nodes
  and 177 supported edges. Repeat load, unrelated sentinel preservation/removal
  and wrong-generation clear no-op passed.
- [x] Guide, saved/implemented skill plan, skill, brief and README discovery links.
- [x] 120 graph tests passed, including 3 skill contract tests; Ruff, Compose config,
  shell syntax, diff check and focused dangerous-call scan passed.
- [x] Independent import review passed with 20 focused tests. Separate advisory
  agent demonstrated known-field citations and missing-field unknowns, with no
  documentation blockers; this was one demonstration, not an LLM benchmark.
- [x] `.devcontainer/neo4j/ACCEPTANCE.md` records the milestone and boundaries.

## Next Steps / Pending Items

- [ ] Restricted Neo4j retrieval facade, continuous privacy revocation, runtime
  evidence import and comparative Graph-RAG evaluation remain separate work.
- [ ] Concurrent import/clear stress and cross-scope rollback tests remain future
  integration coverage. Current evidence graph still lacks full consumer tracing.
- No cloud resources, commits, pushes or global environment changes performed.
