# Session Checkpoint: 2026-08-23 03:50 UTC - `f6a7b8c9`

## Topic
Root Cause Analysis of Evaluation Tracks, Section 15 Troubleshooting Matrix, and MuJoCo/Robosuite 1.4.1 Stability Pinning

## Context & Objectives
Diagnosed and documented why closed-loop evaluation attempts failed across IsaacLab-Arena and Isaac-GR00T simulation tracks. Established distinct operational tracks for native Isaac Sim / PhysX benchmarks vs MuJoCo / LIBERO simulation suites, documented root causes in `isaac-install-plan.md`, and resolved MuJoCo joint traversal assertions.

## Work Accomplished
1. **Root Cause Analysis across 5 Key Failure Modes**:
   - **Failure 1**: Incompatible `robosuite >= 1.5.0` with `mujoco >= 3.0` in LIBERO virtualenv crashing on non-1DoF joints in `get_joint_qpos_addr`.
   - **Failure 2**: Architectural separation between MuJoCo-based GR00T sim benchmarks (`rollout_policy.py`) and Isaac Sim PhysX-based Arena (`policy_runner.py`).
   - **Failure 3**: Argument parsing in Arena `policy_runner.py` rejecting raw Gym spec strings in favor of 17 native task keys (`cube_goal_pose`, `lift_object`, etc.).
   - **Failure 4**: `arena play` defaulting to `zero_action` holding robot motionless.
   - **Failure 5**: Absence of `isaaclab_arena_gr00t` in public Arena repository tree causing `ModuleNotFoundError`.
2. **Master Plan Section 14 & 15 Expansion**:
   - Updated Section 14.3 with clear Dual Simulation & Evaluation Tracks diagram and command runbooks.
   - Added Section 15 Troubleshooting & Lessons Learned Matrix covering all 8 observed failure points and post-execution verification checklist.
3. **Automated LIBERO Compatibility Pin**:
   - Updated `lib/modules/gr00t.sh` (`setup-benchmark` and `rollout`) to automatically enforce `robosuite==1.4.1` and `mujoco==2.3.7` compatibility inside `libero_uv/.venv`.
4. **Task Discovery Optimization**:
   - Updated `./bin/isaac-installer arena list-tasks` to present categorized, valid Arena native tasks.

## Key Files Modified
- `.agents/references/docs/isaac-install-plan.md`: Section 14 & 15 overhaul.
- `.agents/references/isaac-installer/lib/modules/gr00t.sh`: LIBERO pin enforcement and benchmark routing.
- `.agents/references/isaac-installer/lib/modules/isaaclab_arena.sh`: Native task list discovery and option parser fixes.
- `.agents/memory/INDEX.md`: Catalog update.

## Next Steps
- Verify `gr00t rollout --port 5555 --n-episodes 1` completes under the pinned `robosuite==1.4.1` environment.
- Run native Arena benchmarks (`arena run cube_goal_pose --steps 100 --num_envs 8`).
