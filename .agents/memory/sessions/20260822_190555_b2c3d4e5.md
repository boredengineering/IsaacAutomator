---
uuid: "b2c3d4e5-5930-4112-a102-1827364fb403"
short_uuid: "b2c3d4e5"
timestamp: "2026-08-22T19:05:55Z"
topic: "Detached-HEAD Golden Baselines, PyTorch IPC Decoupling & Real-Time Action Blending"
tags: ["detached-heads", "golden-baseline", "pytorch-split", "action-blending", "real-time-chunking", "huggingface-auth"]
---

# Session Memory: Detached-HEAD Golden Baselines, PyTorch IPC Decoupling & Real-Time Action Blending

## Summary
In this session, we codified three critical robustness mechanisms directly into `isaac-install-plan.md` under **Section 5.8**:
1. **Detached-HEAD Golden Baseline vs Standalone Live Workspaces**:
   - Preserved the ability to run exact NVIDIA upstream commit snapshots (`af1bab4` for IsaacLab, `e29d8fc` for Isaac-GR00T) as an immutable baseline to compare against when custom forks experience regressions.
   - Built a 3-mode operational workflow: Golden Baseline (`restore-pinned`), Editable Bridge (`editable-bridge`), and Symlink Redirection (`link-standalone`).
2. **Deep Resolution of the PyTorch Binary Split (`torch 2.11` vs `torch 2.9`)**:
   - Evaluated 4 architectural alternatives (Monolithic env, ZeroMQ Microservice IPC, Shared-Memory POSIX buffer `/dev/shm`, and C++ TensorRT Full-Pipeline).
   - Documented the exact IPC socket contract, heartbeat timeouts (`RCVTIMEO=5000ms`), and symbol isolation guards.
3. **Action Horizon Dynamics, RTC & Temporal Action Blending**:
   - Formulated mathematical action blending (EMA / cubic Hermite splines) across receding execution chunks ($H_e = 8$) to eliminate diffusion boundary jerk.
   - Proved the Real-Time Chunking (RTC $\ge 32$) latency constraint ($L_{\text{infer}} = 27.9\text{ms} < T_{\text{exec}} = 160\text{ms}$).
4. **Hugging Face Token Automation & Offline Mock Modes**:
   - Codified automated `$HF_TOKEN` persistence and `--mock` zero-token offline testing.
