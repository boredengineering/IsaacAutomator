---
uuid: "9ead6140-7d64-476c-8ddd-15b6932f3d8a"
short_uuid: "9ead6140"
timestamp: "2026-09-13T18:09:22Z"
topic: "C3X-only cost direction, retirement and dedicated-project handoff"
tags: [cost, c3x, retirement, devcontainer, planning]
---

# Session Memory: C3X-only costing and project handoff

## Summary

The user selected C3X as the sole future engine, requested removal of competing
tooling and a dedicated-project continuation/devcontainer plan. Active Infracost
code, installer, Docker hooks and cost UI were removed without a substitute.
TerraCost/IBM remain closed historical experiments, not active dependencies.

## Decisions & Rationale

- C3X is selected, not a validated G4 estimator. Prior OAuth and genuine scoped
  GCP import pass; all four G4 Standard/Flex-start estimates still fail.
- Future C3X API/CLI source belongs in separately owned checkouts, with a small
  dedicated workspace and Go/PostgreSQL devcontainer—not wholesale vendoring or
  reuse of Neo4j as PostgreSQL. Later optional Isaac integration is gated on accuracy.
- Retain public fixtures, C3X patches/catalog/audit and original trial receipts;
  historical evidence is not active installation guidance.
- Only instructions/templates were created. No new project/fork, image build,
  running service, cloud action, settings edit, commit or push.

## Completed Tasks

- [x] Removed old cost command, adapter, installer, widget, packaging and five
  retired tests; added seven retirement/UI behavioral tests.
- [x] Updated root/cost/profile docs, closed the alternative evaluation queue and
  revised main roadmap Tasks 28–30 and §13.8.
- [x] Wrote `.agents/references/plans/c3x-dedicated-project-plan.md`: source pins,
  ownership, minimal Go/optional Postgres templates, secret/importer boundary,
  bootstrap, migration/serving separation and CS1–CS9 acceptance sequence.
- [x] Parent reran 271 tests / 12 suites, no skips; source review PASS.
- [x] Six archived metadata records match HEAD byte-for-byte; 72 C3X/public fixture
  files unchanged. Python AST, added-line security scan and diff checks pass.
- [x] Parsed embedded JSON and rendered default/catalog Compose configurations;
  only editor is default, no published ports, internal catalog network, narrowed
  build context. Both shell blocks parse; isolated bootstrap guard cases pass.
- [x] Final independent plan recheck PASS after safety fixes; all blockers and
  documentation suggestions resolved. Source review has one retained non-blocking
  suggestion for broader heuristic-price regression coverage.

## Discovered Gotchas & Caveats

- Initial parent test harness used the TUI-only Python for CLI dependencies,
  omitted `/usr/local/bin` (Terraform), and set APP_DIR to a nonexistent directory.
  Corrected harness only: system Python for CLI, existing TUI venv for Textual,
  allowlisted environment and APP_DIR at the repo. No dependency installs/fixes.
- Plan review found fail-open shell bootstrap. Fixed with fail-fast subshells,
  absolute/nonexistent/non-symlink checks, atomic leaf mkdir and bounded readiness.
  Also removed two stale calculator claims from the profile guide.
- Existing C3X `serve` runs migrations: a read-only DB role needs code/lifecycle
  changes. Dev editor network access is not isolated acceptance. Image availability,
  UID/pgpass behavior and all real devcontainer startup remain unverified.
- Cleanup is repo-scoped; unrelated host tools, shared caches and existing
  containers/devcontainer/agent startup guidance remain unchanged.

## Evidence and Next Steps

- `configs/cost/retirement/`: current test/structural/template checks and reviews.
- `configs/cost/archive/infracost/`: inert historical metadata and hash manifest.
- `configs/cost/c3x-gcp-validation/` and `c3x-oauth/`: unchanged earlier evidence.
- Review/implement the dedicated C3X plan later, qualify all four G4 cases and
  component/lifetime coverage before any new optional Isaac CLI/TUI adapter.
