#!/usr/bin/env python3
"""Read-only cost entry point; run in the environment containing selected inputs.

Unlike provisioning wrappers, never implicitly launch a container, install tools
or translate inaccessible host paths. Inside ./run this same entry point works.
"""
from src.python.cost_command import main

if __name__ == '__main__':
    main()
